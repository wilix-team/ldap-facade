project.description 'Main external api for implementing data sources for ldap'

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}