listener:
  port: 10636
  sslEnabled: false

ldap:
  baseDn: 'dc=wilix,dc=dev'
  servicesBaseDn: 'ou=services,dc=wilix,dc=dev'
  usersBaseDn: 'ou=people,dc=wilix,dc=dev'
  groupsBaseDn: 'ou=groups,dc=wilix,dc=dev'
  userClassName: 'organizationalPerson' # TODO Посмотреть, какой сюда подставить.
  groupClassName: 'groupOfNames'
  mainNameAttribute: 'uid'
  searchCacheExpirationMinutes: 0

storage.user:
  cacheExpirationMinutes: 2
  baseUrl: https://crm.wilix.org